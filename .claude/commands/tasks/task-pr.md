Create pull request.

Steps:

1. Get current branch

2. Generate:

    * PR title
    * PR description

3. Push:
   git push -u origin <branch>

4. Create PR:
   gh pr create --title "<title>" --body "<description>"

Return:

* PR link
* title
